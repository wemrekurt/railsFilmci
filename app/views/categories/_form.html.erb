
  <%= form_for @category do |f| %>
    <%= label_tag(:name, 'Kaegori Adı: ') %>
    <%= f.text_field :name, class: "form-control" %>
    <%= submit_tag "Gönder", class: "btn btn-success" %>
  <% end %>
